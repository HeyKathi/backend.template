# BACKEND_NAME_PLACEHOLDER Documentation

This is a template, please fill with content.

# Anleitung zum Starten des Backends (im Hauptordner)
Befehl: python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload